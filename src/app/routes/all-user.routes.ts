import { Routes } from "@angular/router";
import { AllUserComponent } from '../features/all-user/all-user.component';

export const AllUserRoutes: Routes = [
  { path: '',  component: AllUserComponent },
];
